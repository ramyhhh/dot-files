# Install brew
/usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"

# install iTerm and configure it (default commands)
# ssh keys
# install npm

# secure npm
npm config set ignore-scripts true
